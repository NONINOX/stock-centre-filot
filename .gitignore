# Fichiers et dossiers globaux à ignorer
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Frontend (React - Create React App)
frontend/node_modules
frontend/build
frontend/.pnp
frontend/.pnp.js
frontend/coverage

# Fichiers d'environnement spécifiques au frontend (ne pas commiter les clés secrètes)
frontend/.env.local
frontend/.env.development.local
frontend/.env.test.local
frontend/.env.production.local

# Backend
# Si ton backend est en Node.js et a un dossier node_modules
backend/node_modules

# Si tu as des fichiers d'environnement pour le backend
# backend/.env

# Si ton fichier stock.json est généré dynamiquement ou devient très gros
# et que tu ne veux pas le versionner, tu peux ajouter :
# backend/stock.json
# Mais pour un projet de cette taille, il est souvent utile de le versionner au début.
# Si tu as d'autres fichiers générés par le backend à ignorer, ajoute-les ici.